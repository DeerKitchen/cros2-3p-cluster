# Contributing to cros2-3p-cluster

- One PR per component intake or version bump when possible.
- Update `VERSIONS.md` in the same PR as `src/<component>/` changes.
- Do not add public upstream URLs to release manifests; only this repo URL.
- Run license/CVE checks per `cros2-manifest/docs/THIRD_PARTY_INTAKE_TEMPLATE.md`.
