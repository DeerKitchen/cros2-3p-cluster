# Component versions (cros2-3p-cluster)

| Component | Upstream (reference only) | Pinned version | License | Intake date | Notes |
|-----------|---------------------------|----------------|---------|-------------|-------|
| _(none yet)_ | — | — | — | — | Add rows when syncing into `src/` |

> Upstream URLs in this table are for **intake/audit**. Release `*.repos` must point at **this company repo**, not the public upstream.
